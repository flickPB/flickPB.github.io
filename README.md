# flickPB iOS Universal Links Configuration

This repository hosts the Apple App Site Association (AASA) file for the flickPB iOS app.

## Project Structure
- `.well-known/apple-app-site-association`: The JSON configuration mapping web links to the iOS application bundle.
- `.nojekyll`: Prevents GitHub Pages from ignoring the hidden `.well-known` directory.
